# YouTube Downloader GUI — dépendances
# Installées automatiquement dans le venv au premier lancement (depuis la GUI).

PySide6>=6.6.0
yt-dlp[default]>=2024.1.0
cryptography>=42.0.0

# recommandé : pour « Importer depuis Firefox » (onglet Prérequis)
# pip install browser-cookie3
browser-cookie3>=0.19.0
